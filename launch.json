{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "node",
          "request": "launch",
          "name": "Launch Server",
          "outputCapture": "std",
          "program": "${workspaceFolder}/remote/server.js",
          "cwd": "${workspaceFolder}",
          "outFiles": [
              "${workspaceFolder}/**/*.js"
          ]
          
      }
  ]
}
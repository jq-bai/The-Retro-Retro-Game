import React from 'react';

const GameState = () => {
    return (
        <div className="hero center" id="gameState">
            <h2>Game State Initial</h2>
            <div className="cardGrid" id="cardGrid"></div>
        </div>
    );
};

export default GameState;